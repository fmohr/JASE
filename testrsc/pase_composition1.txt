x_norm = localhost:5000/sklearn.preprocessing::normalize({X=training_data});  
model = sklearn.linear_model.LinearRegression::__construct({,});  
empty = model::fit({X=x_norm});    
prediction = model::predict({X=prediction_data});